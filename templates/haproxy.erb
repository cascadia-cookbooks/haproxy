# Managed by Chef, changes will be overwritten

# Defaults file for HAProxy

<% node['cas_haproxy']['sites'].each do |site| %>
EXTRAOPTS="$EXTRAOPTS -f /etc/haproxy/sites/<%= site[:id] %>.cfg"
<% end %>
